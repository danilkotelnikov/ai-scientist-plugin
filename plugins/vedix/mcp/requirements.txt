# MCP server runtime dependencies for ai-scientist plugin.
#
# The lib uses only stdlib for core functionality. The optional chromadb
# backend enables semantic search in the knowledge store (lazy-imported in
# chroma_store.py). If unavailable, the store falls back to SQLite FTS5.
#
# Per-experiment Python deps live in per-job venvs, not here.

chromadb>=0.4.22
sentence-transformers>=2.5.0

# v2.1.0 strict validation + cross-host dispatch.
#
# bibtexparser: v2 is still a beta line on PyPI (2.0.0b9 latest as of
# 2026-04-28); pip's stable-only resolver under default settings will refuse
# `>=2.0`. We pin to v1.4 stable, which works for the regex-based reference
# I/O the v2.1 cross_validator needs. Bump to >=2.0.0b9 + --pre when v2
# stabilizes.
habanero>=2.3.0
pyalex>=0.17
bibtexparser>=1.4,<3.0
rapidfuzz>=3.0
httpx>=0.27

# v3.0.0 Block 2 — BYOK multi-provider. Each adapter lazily imports its SDK,
# so installing only the SDKs you actually configure is fine. Tests gate on
# `pytest.importorskip(...)` when an SDK is absent.
anthropic>=0.40
openai>=1.55
google-generativeai>=0.8
dashscope>=1.20
gigachat>=0.1.30
mistralai>=1.2
cohere>=5.13
pytest-asyncio>=0.24
respx>=0.21

# v3.0.0 Block 3 — Novel rigor tracks (§4).
# Lazily imported by orchestrator modules; tests gate on `pytest.importorskip`
# so they pass even when a dep is missing locally.
hdbscan>=0.8.33
networkx>=3.0

# v3.0.0 Block 4 — Net-new functionality (§5).
# numerical_audit and reproducibility_audit read CSV artifacts via pandas.
pandas>=2.0

# v3.0.0 Block 5 — Hybrid register discriminator (§5.3) + dataset prep +
# CPU/GPU training. All heavy ML deps are lazy-imported by their respective
# modules; tests use ``pytest.importorskip`` so the unit suite still runs
# without them. Training/inference users install via the bootstrap scripts.
torch>=2.4
transformers>=4.45
safetensors>=0.4.5
accelerate>=0.34
datasketch>=1.6
spacy>=3.7
pdfminer.six>=20240706
lxml>=5.0
beautifulsoup4>=4.12
psutil>=5.9
# fasttext on Windows ships pre-built via the lite wrapper.
fasttext-langdetect>=1.0.5
